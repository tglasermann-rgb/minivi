import { PlaceholderPage } from "@/components/layout/placeholder-page";

export const metadata = { title: "Ventas" };

export default function Page() {
  return <PlaceholderPage title="Ventas" phase={5} description="Órdenes de Shopify (web, tienda y TikTok) y la métrica de ventas por semana." />;
}
